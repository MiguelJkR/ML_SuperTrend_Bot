# ML SuperTrend v51 - GPU Requirements (RTX 4070 Super + i7-14700KF)
# Install with: pip install -r requirements_gpu.txt

# === CORE TRADING ===
requests>=2.31.0
flask>=3.0.0
flask-cors>=4.0.0
numpy>=1.26.0
pandas>=2.1.0

# === ML / DEEP LEARNING (CUDA 12.x) ===
# PyTorch with CUDA — install separately via:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
xgboost>=2.0.0
scikit-learn>=1.3.0
scipy>=1.11.0
hmmlearn>=0.3.0          # Hidden Markov Models para regime detection
PyWavelets>=1.5.0        # Wavelet decomposition multi-escala (DWT)
optuna>=3.4.0            # Bayesian hyperparameter optimization (TPE)

# === TRANSFER LEARNING / NLP (HuggingFace - Gratuito) ===
transformers>=4.35.0     # FinBERT y modelos pre-entrenados
tokenizers>=0.15.0       # Tokenizacion rapida

# === SENTIMENT ANALYSIS ===
vaderSentiment>=3.3.2    # Fallback sentimiento (sin GPU)

# === DATOS DE MERCADO EXTERNOS (Gratuitos) ===
yfinance>=0.2.31         # Yahoo Finance: historicos, indices, commodities
fredapi>=0.5.1           # FRED: datos macroeconomicos US (API key gratis)

# === GPU MONITORING ===
pynvml>=11.5.0           # NVIDIA GPU stats (temp, VRAM, utilization)
gputil>=1.4.0            # GPU utility wrapper

# === TECHNICAL ANALYSIS ===
ta-lib>=0.4.28; platform_system != "Windows"
# On Windows: download TA-Lib wheel from https://github.com/cgohlke/talib-build/releases

# === DATA & VISUALIZATION ===
plotly>=5.18.0
matplotlib>=3.8.0

# === TELEGRAM BOT ===
python-telegram-bot>=20.0

# === UTILITIES ===
python-dotenv>=1.0.0
schedule>=1.2.0
colorama>=0.4.6
