""\"\nML SuperTrend v51 - Enriquecedor de Datos de Mercado (100% Gratuito)\n=====================================================================\nDescarga y procesa datos de múltiples fuentes gratuitas para alimentar\nal modelo LSTM con features adicionales que mejoran las predicciones.\n\nFuentes Gratuitas:\n  1. yfinance      — Datos históricos OHLCV, índices, commodities (Yahoo Finance)\n  2. FRED API      — Datos macroeconómicos (tasas de interés, inflación, empleo)\n  3. Alpha Vantage — Indicadores técnicos y fundamentales (free: 25 req/día)\n  4. Exchange Rates — Tipos de cambio cruzados para correlaciones\n\nFeatures que genera:\n  - Índices de mercado (S&P500, VIX, DXY) como contexto global\n  - Correlaciones entre pares\n  - Datos macro (tasas Fed, inflación, desempleo)\n  - Volatilidad implícita (VIX)\n  - Fuerza del dólar (DXY)\n  - Spread de bonos (2Y-10Y yield curve)\n\nCache:\n  - Cachea datos en disco para no repetir requests\n  - Refresca cada 4 horas (configurable)\n\nUso:\n    enricher = MarketDataEnricher()\n    features = enricher.get_enriched_features(\"EUR_USD\")\n    # → {\"vix\": 18.5, \"dxy\": 104.2, \"sp500_change\": 0.3, ...}\n\"\"\"\n\nimport logging\nimport json\nimport os\nimport time\nimport numpy as np\nfrom datetime import datetime, timedelta, timezone\nfrom typing import Dict, List, Optional, Tuple\nfrom pathlib import Path\n\nlogger = logging.getLogger(__name__)\n\n# =====================================================================\n# CACHE CONFIG\n# =====================================================================\nCACHE_DIR = os.path.join(os.path.dirname(os.path.abspath(__file__)), \"market_cache\")\nCACHE_TTL_HOURS = 4  # Refrescar datos cada 4 horas\nFRED_API_KEY = os.getenv(\"FRED_API_KEY\", \"\")  # Gratis: https://fred.stlouisfed.org/docs/api/api_key.html\nALPHA_VANTAGE_KEY = os.getenv(\"ALPHA_VANTAGE_KEY\", \"demo\")  # Gratis: https://www.alphavantage.co/support/#api-key\n\n# Tickers de referencia para contexto global\nREFERENCE_TICKERS = {\n    \"sp500\": \"^GSPC\",       # S&P 500\n    \"vix\": \"^VIX\",          # Volatilidad implícita\n    \"dxy\": \"DX-Y.NYB\",      # Índice del dólar\n    \"gold\": \"GC=F\",         # Oro (futuros)\n    \"oil\": \"CL=F\",          # Petróleo WTI\n    \"tnx\": \"^TNX\",          # Rendimiento bonos 10Y US\n    \"tyx\": \"^TYX\",          # Rendimiento bonos 30Y US\n    \"nikkei\": \"^N225\",      # Nikkei 225\n    \"dax\": \"^GDAXI\",        # DAX alemán\n    \"btc\": \"BTC-USD\",       # Bitcoin\n}\n\n# Series FRED para datos macro\nFRED_SERIES = {\n    \"fed_rate\": \"FEDFUNDS\",       # Tasa de la Fed\n    \"inflation\": \"CPIAUCSL\",      # CPI (inflación)\n    \"unemployment\": \"UNRATE\",     # Tasa de desempleo\n    \"yield_2y\": \"DGS2\",           # Rendimiento bonos 2Y\n    \"yield_10y\": \"DGS10\",         # Rendimiento bonos 10Y\n    \"yield_spread\": \"T10Y2Y\",     # Spread 10Y-2Y (inversión = recesión)\n    \"m2_money\": \"M2SL\",           # Masa monetaria M2\n    \"consumer_sentiment\": \"UMCSENT\",  # Sentimiento del consumidor\n}\n\n# Mapeo de instrumentos OANDA → tickers de Yahoo\nOANDA_TO_YAHOO = {\n    \"EUR_USD\": \"EURUSD=X\",\n    \"GBP_USD\": \"GBPUSD=X\",\n    \"USD_JPY\": \"USDJPY=X\",\n    \"USD_CHF\": \"USDCHF=X\",\n    \"AUD_USD\": \"AUDUSD=X\",\n    \"NZD_USD\": \"NZDUSD=X\",\n    \"USD_CAD\": \"USDCAD=X\",\n    \"XAU_USD\": \"GC=F\",\n}\n\n\nclass MarketDataEnricher:\n    \"\"\"\n    Enriquece datos de trading con información externa gratuita.\n    Auto-cachea para respetar límites de APIs gratuitas.\n    \"\"\"\n\n    def __init__(self, cache_dir: str = None):\n        self.cache_dir = cache_dir or CACHE_DIR\n        Path(self.cache_dir).mkdir(parents=True, exist_ok=True)\n\n        self._yf = None\n        self._yf_available = False\n        self._fred_available = False\n        self._av_available = False\n\n        # Intentar importar yfinance\n        try:\n            import yfinance as yf\n            self._yf = yf\n            self._yf_available = True\n            logger.info(\"yfinance disponible — datos de Yahoo Finance activados\")\n        except ImportError:\n            logger.warning(\"yfinance no instalado. Instala con: pip install yfinance\")\n\n        # Cache en memoria\n        self._cache: Dict[str, Dict] = {}\n        self._last_refresh: Dict[str, float] = {}\n\n    # ─────────────────────────────────────────────────────────\n    # YAHOO FINANCE (yfinance) — 100% gratis, sin API key\n    # ─────────────────────────────────────────────────────────\n    def get_market_context(self) -> Dict:\n        \"\"\"\n        Obtener contexto global del mercado desde Yahoo Finance.\n        Returns: {vix, dxy, sp500_change, gold_change, oil_change, ...}\n        \"\"\"\n        cache_key = \"market_context\"\n        cached = self._get_cache(cache_key)\n        if cached:\n            return cached\n\n        if not self._yf_available:\n            return self._empty_market_context()\n\n        context = {}\n        try:\n            for name, ticker in REFERENCE_TICKERS.items():\n                try:\n                    data = self._yf.download(ticker, period=\"5d\", interval=\"1d\",\n                                              progress=False, timeout=10)\n                    if data is not None and len(data) >= 2:\n                        last_close = float(data['Close'].iloc[-1])\n                        prev_close = float(data['Close'].iloc[-2])\n                        change_pct = ((last_close - prev_close) / prev_close) * 100\n\n                        context[name] = last_close\n                        context[f\"{name}_change\"] = round(change_pct, 3)\n                    else:\n                        context[name] = 0\n                        context[f\"{name}_change\"] = 0\n                except Exception as e:\n                    logger.debug(f\"Error obteniendo {name}: {e}\")\n                    context[name] = 0\n                    context[f\"{name}_change\"] = 0\n\n            # Calcular features derivadas\n            if context.get(\"vix\", 0) > 0:\n                # Nivel de miedo del mercado\n                vix = context[\"vix\"]\n                context[\"market_fear\"] = (\n                    \"EXTREME\" if vix > 35 else\n                    \"HIGH\" if vix > 25 else\n                    \"MODERATE\" if vix > 18 else\n                    \"LOW\"\n                )\n                context[\"vix_norm\"] = min(vix / 50.0, 1.0)  # Normalizado 0-1\n\n            # Fuerza del dólar normalizada\n            if context.get(\"dxy\", 0) > 0:\n                context[\"dxy_norm\"] = (context[\"dxy\"] - 90) / 20  # ~90-110 range → 0-1\n\n            # Yield curve (inversión = señal de recesión)\n            tnx = context.get(\"tnx\", 0)\n            if tnx > 0:\n                context[\"yield_10y\"] = tnx\n\n            context[\"timestamp\"] = datetime.now(timezone.utc).isoformat()\n            self._set_cache(cache_key, context)\n\n        except Exception as e:\n            logger.error(f\"Error obteniendo contexto de mercado: {e}\")\n            return self._empty_market_context()\n\n        return context\n\n    def get_pair_history(self, instrument: str, period: str = \"3mo\") -> Optional[np.ndarray]:\n        \"\"\"\n        Obtener historia de precios de un par desde Yahoo Finance.\n        Útil para calcular correlaciones y patrones de largo plazo.\n        \"\"\"\n        if not self._yf_available:\n            return None\n\n        yahoo_ticker = OANDA_TO_YAHOO.get(instrument)\n        if not yahoo_ticker:\n            return None\n\n        cache_key = f\"history_{instrument}_{period}\"\n        cached = self._get_cache(cache_key)\n        if cached is not None:\n            return np.array(cached)\n\n        try:\n            data = self._yf.download(yahoo_ticker, period=period, interval=\"1d\",\n                                      progress=False, timeout=15)\n            if data is not None and len(data) > 0:\n                closes = data['Close'].values.tolist()\n                self._set_cache(cache_key, closes)\n                return np.array(closes)\n        except Exception as e:\n            logger.debug(f\"Error obteniendo historia {instrument}: {e}\")\n\n        return None\n\n    def get_correlations(self, instrument: str) -> Dict:\n        \"\"\"\n        Calcular correlaciones del instrumento con índices de referencia.\n        Útil para saber si el par se mueve con/contra el mercado.\n        \"\"\"\n        cache_key = f\"correlations_{instrument}\"\n        cached = self._get_cache(cache_key)\n        if cached:\n            return cached\n\n        pair_data = self.get_pair_history(instrument, \"3mo\")\n        if pair_data is None or len(pair_data) < 20:\n            return {}\n\n        correlations = {}\n        ref_tickers = {\"sp500\": \"^GSPC\", \"gold\": \"GC=F\", \"dxy\": \"DX-Y.NYB\", \"oil\": \"CL=F\"}\n\n        for name, ticker in ref_tickers.items():\n            try:\n                if self._yf_available:\n                    ref_data = self._yf.download(ticker, period=\"3mo\", interval=\"1d\",\n                                                  progress=False, timeout=10)\n                    if ref_data is not None and len(ref_data) >= 20:\n                        ref_closes = ref_data['Close'].values\n                        # Alinear longitudes\n                        min_len = min(len(pair_data), len(ref_closes))\n                        if min_len >= 20:\n                            corr = np.corrcoef(pair_data[-min_len:], ref_closes[-min_len:])[0, 1]\n                            correlations[name] = round(float(corr), 3)\n            except Exception:\n                pass\n\n        self._set_cache(cache_key, correlations)\n        return correlations\n\n    # ─────────────────────────────────────────────────────────\n    # FRED API — Gratis con API key (https://fred.stlouisfed.org)\n    # ─────────────────────────────────────────────────────────\n    def get_macro_data(self) -> Dict:\n        \"\"\"\n        Obtener datos macroeconómicos desde FRED.\n        Requiere FRED_API_KEY (gratuita, registrarse en fred.stlouisfed.org).\n        Sin API key, retorna datos vacíos.\n        \"\"\"\n        cache_key = \"macro_data\"\n        cached = self._get_cache(cache_key, ttl_hours=24)  # Macro data cambia lento\n        if cached:\n            return cached\n\n        if not FRED_API_KEY:\n            logger.info(\"FRED_API_KEY no configurada. Datos macro deshabilitados. \"\n                       \"Registrate gratis en https://fred.stlouisfed.org/docs/api/api_key.html\")\n            return {}\n\n        macro = {}\n        try:\n            import urllib.request\n            import urllib.parse\n\n            for name, series_id in FRED_SERIES.items():\n                try:\n                    url = (f\"https://api.stlouisfed.org/fred/series/observations?\"\n                           f\"series_id={series_id}&api_key={FRED_API_KEY}\"\n                           f\"&file_type=json&sort_order=desc&limit=5\")\n\n                    req = urllib.request.Request(url)\n                    with urllib.request.urlopen(req, timeout=10) as resp:\n                        data = json.loads(resp.read().decode('utf-8'))\n                        observations = data.get(\"observations\", [])\n                        if observations:\n                            # Tomar el valor más reciente que no sea \".\"\n                            for obs in observations:\n                                val = obs.get(\"value\", \".\")\n                                if val != \".\":\n                                    macro[name] = float(val)\n                                    macro[f\"{name}_date\"] = obs.get(\"date\", \"\")\n                                    break\n                except Exception as e:\n                    logger.debug(f\"FRED {name}: {e}\")\n\n            # Calcular spread yield curve si tenemos los datos\n            if \"yield_2y\" in macro and \"yield_10y\" in macro:\n                macro[\"yield_curve_spread\"] = round(macro[\"yield_10y\"] - macro[\"yield_2y\"], 3)\n                macro[\"yield_curve_inverted\"] = macro[\"yield_curve_spread\"] < 0\n\n            macro[\"timestamp\"] = datetime.now(timezone.utc).isoformat()\n            self._set_cache(cache_key, macro, ttl_hours=24)\n\n        except Exception as e:\n            logger.error(f\"Error obteniendo datos FRED: {e}\")\n\n        return macro\n\n    # ─────────────────────────────────────────────────────────\n    # FEATURES COMBINADAS PARA EL MODELO\n    # ─────────────────────────────────────────────────────────\n    def get_enriched_features(self, instrument: str) -> Dict:\n        \"\"\"\n        Obtener TODAS las features externas combinadas para un instrumento.\n        Este es el método principal que el modelo LSTM debe llamar.\n\n        Returns: dict con features normalizadas listas para el modelo:\n            - vix_norm (0-1): volatilidad implícita\n            - dxy_norm (0-1): fuerza del dólar\n            - sp500_momentum: cambio % S&P500\n            - gold_momentum: cambio % oro\n            - oil_momentum: cambio % petróleo\n            - market_fear_score (0-1): nivel de miedo\n            - yield_curve_signal (-1 a 1): señal de curva de rendimiento\n            - corr_sp500: correlación con S&P500\n            - corr_gold: correlación con oro\n            - corr_dxy: correlación con DXY\n            - macro_score (0-1): salud macroeconómica general\n        \"\"\"\n        features = {}\n\n        # 1. Contexto de mercado (Yahoo Finance)\n        ctx = self.get_market_context()\n        features[\"vix_norm\"] = ctx.get(\"vix_norm\", 0.5)\n        features[\"dxy_norm\"] = np.clip(ctx.get(\"dxy_norm\", 0.5), 0, 1)\n        features[\"sp500_momentum\"] = np.clip(ctx.get(\"sp500_change\", 0) / 3.0, -1, 1)\n        features[\"gold_momentum\"] = np.clip(ctx.get(\"gold_change\", 0) / 3.0, -1, 1)\n        features[\"oil_momentum\"] = np.clip(ctx.get(\"oil_change\", 0) / 5.0, -1, 1)\n\n        # Fear score: 0=calm, 1=panic\n        fear_map = {\"LOW\": 0.1, \"MODERATE\": 0.3, \"HIGH\": 0.6, \"EXTREME\": 0.9}\n        features[\"market_fear_score\"] = fear_map.get(ctx.get(\"market_fear\", \"MODERATE\"), 0.3)\n\n        # 2. Correlaciones\n        corrs = self.get_correlations(instrument)\n        features[\"corr_sp500\"] = corrs.get(\"sp500\", 0)\n        features[\"corr_gold\"] = corrs.get(\"gold\", 0)\n        features[\"corr_dxy\"] = corrs.get(\"dxy\", 0)\n\n        # 3. Datos macro (FRED)\n        macro = self.get_macro_data()\n        if macro:\n            # Yield curve signal: positivo = expansión, negativo = contracción\n            spread = macro.get(\"yield_curve_spread\", 0)\n            features[\"yield_curve_signal\"] = np.clip(spread / 2.0, -1, 1)\n\n            # Macro health score (simple composite)\n            unemployment = macro.get(\"unemployment\", 5.0)\n            fed_rate = macro.get(\"fed_rate\", 5.0)\n            features[\"macro_score\"] = np.clip(1.0 - (unemployment / 10.0), 0, 1)\n        else:\n            features[\"yield_curve_signal\"] = 0\n            features[\"macro_score\"] = 0.5\n\n        return features\n\n    def get_feature_names(self) -> List[str]:\n        \"\"\"Lista de nombres de features externas disponibles.\"\"\"\n        return [\n            \"vix_norm\", \"dxy_norm\", \"sp500_momentum\", \"gold_momentum\",\n            \"oil_momentum\", \"market_fear_score\", \"yield_curve_signal\",\n            \"corr_sp500\", \"corr_gold\", \"corr_dxy\", \"macro_score\"\n        ]\n\n    # ─────────────────────────────────────────────────────────\n    # CACHE\n    # ─────────────────────────────────────────────────────────\n    def _get_cache(self, key: str, ttl_hours: float = None) -> Optional[Dict]:\n        \"\"\"Obtener datos del cache si no han expirado.\"\"\"\n        ttl = ttl_hours or CACHE_TTL_HOURS\n\n        # Memory cache first\n        if key in self._cache:\n            age_hours = (time.time() - self._last_refresh.get(key, 0)) / 3600\n            if age_hours < ttl:\n                return self._cache[key]\n\n        # Disk cache\n        cache_file = os.path.join(self.cache_dir, f\"{key}.json\")\n        if os.path.exists(cache_file):\n            try:\n                mtime = os.path.getmtime(cache_file)\n                age_hours = (time.time() - mtime) / 3600\n                if age_hours < ttl:\n                    with open(cache_file, 'r') as f:\n                        data = json.load(f)\n                    self._cache[key] = data\n                    self._last_refresh[key] = mtime\n                    return data\n            except Exception:\n                pass\n\n        return None\n\n    def _set_cache(self, key: str, data, ttl_hours: float = None):\n        \"\"\"Guardar datos en cache (memoria + disco).\"\"\"\n        self._cache[key] = data\n        self._last_refresh[key] = time.time()\n\n        try:\n            cache_file = os.path.join(self.cache_dir, f\"{key}.json\")\n            with open(cache_file, 'w') as f:\n                json.dump(data, f, default=str)\n        except Exception as e:\n            logger.debug(f\"Cache write error: {e}\")\n\n    def _empty_market_context(self) -> Dict:\n        \"\"\"Contexto vacío cuando no hay datos disponibles.\"\"\"\n        return {\n            \"vix\": 0, \"vix_norm\": 0.5, \"dxy\": 0, \"dxy_norm\": 0.5,\n            \"sp500\": 0, \"sp500_change\": 0, \"gold\": 0, \"gold_change\": 0,\n            \"oil\": 0, \"oil_change\": 0, \"market_fear\": \"MODERATE\",\n            \"timestamp\": datetime.now(timezone.utc).isoformat(),\n        }\n\n    def get_status(self) -> Dict:\n        \"\"\"Estado del enriquecedor para dashboard.\"\"\"\n        return {\n            \"yfinance\": self._yf_available,\n            \"fred\": bool(FRED_API_KEY),\n            \"alpha_vantage\": ALPHA_VANTAGE_KEY != \"demo\",\n            \"cache_size\": len(self._cache),\n            \"cache_dir\": self.cache_dir,\n            \"features_available\": self.get_feature_names(),\n        }\n\n\nif __name__ == \"__main__\":\n    enricher = MarketDataEnricher()\n    print(\"Status:\", json.dumps(enricher.get_status(), indent=2))\n\n    print(\"\\nMarket Context:\")\n    ctx = enricher.get_market_context()\n    for k, v in ctx.items():\n        print(f\"  {k}: {v}\")\n\n    print(\"\\nEnriched Features (EUR_USD):\")\n    features = enricher.get_enriched_features(\"EUR_USD\")\n    for k, v in features.items():\n        print(f\"  {k}: {v}\")\n